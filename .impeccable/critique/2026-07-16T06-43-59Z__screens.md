---
target: the whole app (screens/)
total_score: 24
p0_count: 1
p1_count: 3
timestamp: 2026-07-16T06-43-59Z
slug: screens
---
Method: dual-agent (A: design review · B: detector + evidence), isolated + parallel.

# Design Health Score: 24/40 — Acceptable

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 3 | Timers/progress solid; autosave invisible |
| 2 | Match System / Real World | 2 | 'Primed' and 'Ready' render in the same green; RPE unglossed |
| 3 | User Control and Freedom | 3 | Cancel/Back good; no undo on mis-tapped set type |
| 4 | Consistency and Standards | 2 | Two primary buttons; paywall has 0 t() calls vs 33-156 elsewhere |
| 5 | Error Prevention | 3 | Strong: dup-exercise guard, draft flush, double-tap guard |
| 6 | Recognition Rather Than Recall | 2 | Set types W/D/F/· cycle with no legend, mid-set |
| 7 | Flexibility and Efficiency | 3 | Swipe pager, dot jump, search/filter, plate calc |
| 8 | Aesthetic and Minimalist | 2 | TodayScreen ~9 sections at equal weight |
| 9 | Error Recovery | 2 | Save failure = generic alert, no retry |
| 10 | Help and Documentation | 2 | Nothing explains set types, RPE, or what "Adjusted" changed |
| **Total** | | **24/40** | **Acceptable** |

## Anti-Patterns Verdict
Not AI-slop — reads as "engineer shipped without a designer". Real tells: 5 side-stripe
borders (banned), 29 uppercase tracked eyebrows across 14 files, Alert.alert everywhere
on a Play-only app (no Material).

DETECTOR: 0 findings, exit 0 — NOT a clean bill of health. detect.mjs scans .jsx but its
rules target CSS strings/HTML classes, which don't exist in RN StyleSheet objects. True-
negative from rule mismatch. Browser overlay: FAILED (port 8081 occupied, non-interactive
mode wouldn't take the fallback prompt). No screenshots, no injection.

CORRECTION: RecoveryCheckIn buttons are 44pt+ with hitSlop={8}, NOT 34pt. Prior note stale.

## What's Working
1. Draft persistence is best-in-category (WorkoutExecutionScreen.jsx:199-224) — debounced
   autosave + sync ref + AppState flush incl. finished-but-unsaved. Invisible to users.
2. Slot-vs-exercise volume semantics on swap (:346) — real training theory. Invisible.
3. Error prevention throughout — dup guard, double-tap guard, set-index preservation.

## Priority Issues

[P0] The Coach chat has no chat. conversationHistory is sent to the model (CoachScreen.jsx:798)
and gates Reset (:888) but is NEVER rendered. setAnswer(null) (:791) wipes the prior answer on
every ask; only a single `answer` renders (:983). The only paid feature, at $14.99/mo vs Fitbod,
is a form that forgets on screen while remembering on the server. VERIFIED by controller.
Fix: render conversationHistory as a scrolling thread. Command: /impeccable craft

[P1] Paywall is the least-finished screen. Says "Apple ID" on an Android-only app
(PremiumPaywall.jsx:111); only screen with 0 i18n; SAVE 50% hardcoded; never mentions the
science that is the entire moat. Command: /impeccable clarify

[P1] TodayScreen has no hierarchy. ~9 equal-weight sections; Start competes with a deload card
carrying a 5-step list + science paragraph + 11-chip grid. Command: /impeccable layout

[P1] Set types unlearnable mid-set. W/D/F/· cycling 4 hidden states, no legend, 28x36pt. A
mis-tap silently relabels a working set and corrupts the volume math driving the program.
Command: /impeccable clarify

[P2] Token file orphaned. 1,400 raw hex literals (84 distinct) in screens/*.jsx; only 1 of 24
files imports lib/theme. 17 distinct borderRadius values (no scale); 62 distinct *Btn style
names; 8 near-identical dark surface tones vs 4 tokens. Today's contrast fix cannot hold.
Command: /impeccable extract

## Persona Red Flags
Sam (Accessibility): ZERO accessibilityLabel/Role/accessible across 24 screens / 405
touchables, many icon-only. Screen-reader users cannot use this app at all.
Casey (Distracted Mobile — the actual user): RPE row = 10 buttons at ~32pt at peak fatigue.
finishBtn is grey #2C2C35 (secondary treatment for the primary action) beside an identical
grey Coach button.
Jordan (First-Timer): Meets the readiness sheet before ever finishing a workout; it may offer
to remove a set. Then W/D/F with no legend.

## Minor Observations
- progressFill white vs dotIndicatorDone green — two colours for "progress", 10 lines apart
- #E4E4E7 vs approved #E4E4E8; unlisted #F1F0F5 — copy-paste drift
- parseRestSeconds matches en-dashes only; hyphen falls through to 120s silently
- ActivityIndicator in 6/24 screens; error handling absent from 15 incl. CoachScreen, PremiumPaywall
- Muscle-recovery thresholds cited to Schoenfeld in a CODE COMMENT — that citation is the product

## Questions
1. If the Coach's transcript were the screen, not a card on it, would $14.99 feel different?
2. What if the science were the interface instead of a footnote? Fitbod cannot fake a
   peer-reviewed library; you have one and hide it in 11px grey.
3. Why does a first-timer meet the readiness sheet before their first workout?
